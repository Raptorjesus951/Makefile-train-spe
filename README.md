# Makefile-train-spe
Si ca n'est pas precise on considere que les Makefile doivent avoir comme premiere regle une regle qui permet de creer un binaire depuis des objets (\*.o) et que ces objets doivent etre chacun issue d'un fichier source (\*.c).
